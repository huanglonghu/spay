<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <variable
            name="bean"
            type="com.example.spay.bean.MobileRechargeList.ResultBean" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_mobile_recharge"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="10dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{bean.rechargeAmount+@string/yuan}"
            android:textColor="@drawable/bg_textcolor"
            android:textSize="20sp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{@string/sj+bean.sellAmount+@string/yuan}"
            android:textColor="@drawable/bg_textcolor"
            android:textSize="13sp" />

    </LinearLayout>
</layout>