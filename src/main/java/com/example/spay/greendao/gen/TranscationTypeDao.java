package com.example.spay.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.example.spay.greendao.entity.TranscationType;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "TRANSCATION_TYPE".
*/
public class TranscationTypeDao extends AbstractDao<TranscationType, Void> {

    public static final String TABLENAME = "TRANSCATION_TYPE";

    /**
     * Properties of entity TranscationType.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property TransationName = new Property(0, String.class, "transationName", false, "TRANSATION_NAME");
        public final static Property TransationId = new Property(1, String.class, "transationId", false, "TRANSATION_ID");
    }


    public TranscationTypeDao(DaoConfig config) {
        super(config);
    }
    
    public TranscationTypeDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"TRANSCATION_TYPE\" (" + //
                "\"TRANSATION_NAME\" TEXT," + // 0: transationName
                "\"TRANSATION_ID\" TEXT);"); // 1: transationId
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"TRANSCATION_TYPE\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, TranscationType entity) {
        stmt.clearBindings();
 
        String transationName = entity.getTransationName();
        if (transationName != null) {
            stmt.bindString(1, transationName);
        }
 
        String transationId = entity.getTransationId();
        if (transationId != null) {
            stmt.bindString(2, transationId);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, TranscationType entity) {
        stmt.clearBindings();
 
        String transationName = entity.getTransationName();
        if (transationName != null) {
            stmt.bindString(1, transationName);
        }
 
        String transationId = entity.getTransationId();
        if (transationId != null) {
            stmt.bindString(2, transationId);
        }
    }

    @Override
    public Void readKey(Cursor cursor, int offset) {
        return null;
    }    

    @Override
    public TranscationType readEntity(Cursor cursor, int offset) {
        TranscationType entity = new TranscationType( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // transationName
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1) // transationId
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, TranscationType entity, int offset) {
        entity.setTransationName(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setTransationId(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
     }
    
    @Override
    protected final Void updateKeyAfterInsert(TranscationType entity, long rowId) {
        // Unsupported or missing PK type
        return null;
    }
    
    @Override
    public Void getKey(TranscationType entity) {
        return null;
    }

    @Override
    public boolean hasKey(TranscationType entity) {
        // TODO
        return false;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
