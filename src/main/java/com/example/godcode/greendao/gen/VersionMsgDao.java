package com.example.godcode.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.example.godcode.greendao.entity.VersionMsg;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "VERSION_MSG".
*/
public class VersionMsgDao extends AbstractDao<VersionMsg, Void> {

    public static final String TABLENAME = "VERSION_MSG";

    /**
     * Properties of entity VersionMsg.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property VersionCode = new Property(0, String.class, "versionCode", false, "VERSION_CODE");
        public final static Property VersionDes = new Property(1, String.class, "versionDes", false, "VERSION_DES");
    }


    public VersionMsgDao(DaoConfig config) {
        super(config);
    }
    
    public VersionMsgDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"VERSION_MSG\" (" + //
                "\"VERSION_CODE\" TEXT," + // 0: versionCode
                "\"VERSION_DES\" TEXT);"); // 1: versionDes
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"VERSION_MSG\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, VersionMsg entity) {
        stmt.clearBindings();
 
        String versionCode = entity.getVersionCode();
        if (versionCode != null) {
            stmt.bindString(1, versionCode);
        }
 
        String versionDes = entity.getVersionDes();
        if (versionDes != null) {
            stmt.bindString(2, versionDes);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, VersionMsg entity) {
        stmt.clearBindings();
 
        String versionCode = entity.getVersionCode();
        if (versionCode != null) {
            stmt.bindString(1, versionCode);
        }
 
        String versionDes = entity.getVersionDes();
        if (versionDes != null) {
            stmt.bindString(2, versionDes);
        }
    }

    @Override
    public Void readKey(Cursor cursor, int offset) {
        return null;
    }    

    @Override
    public VersionMsg readEntity(Cursor cursor, int offset) {
        VersionMsg entity = new VersionMsg( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // versionCode
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1) // versionDes
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, VersionMsg entity, int offset) {
        entity.setVersionCode(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setVersionDes(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
     }
    
    @Override
    protected final Void updateKeyAfterInsert(VersionMsg entity, long rowId) {
        // Unsupported or missing PK type
        return null;
    }
    
    @Override
    public Void getKey(VersionMsg entity) {
        return null;
    }

    @Override
    public boolean hasKey(VersionMsg entity) {
        // TODO
        return false;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
